{/* prettier-ignore-start */}

import { radiusValues } from 'extraction-ui';

## Shapes

Use the <code className='nextra-code'>rounded-{'{'}size{'}'}</code> class to control the component border radius. Shape size values can include: {radiusValues.filter((r) => r!== 'DEFAULT').map((radii, index) => (<code key={index} className='nextra-code mx-0.5'>{radii}</code>))}

For more information on all available radius utilities, visit [Tailwind](https://tailwindcss.com/docs/border-radius)

{/* prettier-ignore-end */}
