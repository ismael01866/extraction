import { Field, Password } from 'extraction-ui';

import data from '../data.json';
import { cfg } from './input';

export const Component = (
  <Password className='w-64'>
    <Password.Input placeholder='Enter your password' />
    <Password.Toggle>
      <Password.Icon />
    </Password.Toggle>
  </Password>
);

export const ComponentValid = (
  <Field className='w-64'>
    <Field.Label htmlFor='name'>
      Password
      <Field.RequiredIndicator />
    </Field.Label>
    <Field.Helper>{`The quick brown fox jumps over`}</Field.Helper>
    <Password className='valid'>
      <Password.Input
        placeholder='Enter your password'
        value='12345'
        readOnly
      />
      <Password.Toggle>
        <Password.Icon />
      </Password.Toggle>
    </Password>
    <Field.Success>{`Password is valid`}</Field.Success>
  </Field>
);

export const ComponentInvalid = (
  <Field className='w-64'>
    <Field.Label htmlFor='name'>
      Password
      <Field.RequiredIndicator />
    </Field.Label>
    <Field.Helper>{`The quick brown fox jumps over`}</Field.Helper>
    <Password className='invalid'>
      <Password.Input placeholder='Enter your password' />
      <Password.Toggle>
        <Password.Icon />
      </Password.Toggle>
    </Password>
    <Field.Error>{`Password is required`}</Field.Error>
  </Field>
);

export const ComponentDisabled = (
  <Password className='disabled variant-surface w-64'>
    <Password.Input placeholder='Enter your password' />
    <Password.Toggle>
      <Password.Icon />
    </Password.Toggle>
  </Password>
);

export const Wrapper = ({ children }) => children;

# Password

{data.password.description} <br/>For more information on this component configuration, visit [Radix UI](https://www.radix-ui.com/primitives/docs/components/password-toggle-field)

<Usage
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Disabled

<Usage
  components={{
    usage: ComponentDisabled,
    wrapper: Wrapper,
  }}
  disabled
/>

## Valid

<Usage
  components={{
    usage: ComponentValid,
    wrapper: Wrapper,
  }}
  valid
/>

## Invalid

<Usage
  components={{
    usage: ComponentInvalid,
    wrapper: Wrapper,
  }}
  invalid
/>

## Sizes

<Size
  config={cfg}
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Variants

<Variants
  config={cfg}
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Customizing

```sass filename="globals.css"
:root {
  --ex-password-toggle-cursor: default;

  --ex-password-font-family: var(--font-sans);
  --ex-password-font-weight: var(--font-weight-normal);

  --ex-password-font-size-sm: var(--text-xs);
  --ex-password-font-size-md: var(--text-sm);
  --ex-password-font-size-lg: var(--text-base);

  --ex-password-height-sm: calc(var(--spacing) * 9);
  --ex-password-height-md: calc(var(--spacing) * 10);
  --ex-password-height-lg: calc(var(--spacing) * 11);

  --ex-password-px-sm: calc(var(--spacing) * 3.5);
  --ex-password-px-md: calc(var(--spacing) * 4);
  --ex-password-px-lg: calc(var(--spacing) * 4.5);

  --ex-password-radius-sm: var(--radius-sm);
  --ex-password-radius-md: var(--radius-sm);
  --ex-password-radius-lg: var(--radius-sm);
}
```

## API

For more information on this component configuration, visit [Radix UI](https://www.radix-ui.com/primitives/docs/components/password-toggle-field#api-reference)

<Api config={cfg} />
