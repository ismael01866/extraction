import { IconButton, Toggle } from 'extraction-ui';
import { LuMail } from 'react-icons/lu';

import data from '../data.json';
import { toggleCfg } from './toggle';

import { ToggleControlledExample, ToggleUncontrolledExample } from './examples/toggle-example';

export const Component = (
  <Toggle asChild>
    <IconButton
      className={`variant-outline data-[state='on']:variant-solid`}
      aria-label='toggle button'
    >
      <LuMail />
    </IconButton>
  </Toggle>
);

export const ComponentDisabled = (
  <Toggle asChild disabled>
    <IconButton
      className={`variant-outline data-[state='on']:variant-solid`}
      aria-label='toggle button'
    >
      <LuMail />
    </IconButton>
  </Toggle>
);

export const Wrapper = ({ children }) => children;

# Toggle

{data.toggle.description} <br/>For more information on this component configuration, visit [Radix UI](https://www.radix-ui.com/primitives/docs/components/toggle)

<Usage
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Disabled

<Usage
  components={{
    usage: ComponentDisabled,
    wrapper: Wrapper,
  }}
  disabledNative
/>

## Controlled

Use the `pressed` and `onPressedChange` props when you need to control the value externally.

<Usage
  components={{
    usage: <ToggleControlledExample />,
    wrapper: Wrapper,
  }}
  serializedCode={`const Example = () => {
    const [pressed, setPressed] = useState(false);
    return (
      <Toggle pressed={pressed} onPressedChange={(value) => setPressed(value)} asChild>
        <IconButton
          className={\`variant-outline data-[state='on']:variant-solid\`}
          aria-label="toggle button"
        >
          <LuMail />
        </IconButton>
      </Toggle>
    );
  };`}
/>

## Uncontrolled

Use the `name` prop to include the value in the form submission.

<Usage
  components={{
    usage: <ToggleUncontrolledExample />,
    wrapper: Wrapper,
  }}
  serializedCode={`<form
    onSubmit={(event) => {
      event.preventDefault();
      const formData = new FormData(event.currentTarget);
      console.log(formData.get('toggle'));
  }}>
    <Toggle name="toggle" asChild>
      <IconButton
        className={\`variant-outline data-[state='on']:variant-solid\`}
        aria-label="toggle button"
      >
        <LuMail />
      </IconButton>
    </Toggle>
    <br />
    <Button type="submit" className="palette-neutral variant-surface">
      Submit
    </Button>
  </form>`}
/>

## API

For more information on this component configuration, visit [Radix UI](https://www.radix-ui.com/primitives/docs/components/toggle#api-reference)

### Toggle

<Api config={toggleCfg} />
