import { Button } from 'extraction-ui';
import { LuMail } from 'react-icons/lu';

import data from '../data.json';
import { cfg } from './button';

export const Component = <Button>{`Button`}</Button>;

export const ComponentWithIcon = (
  <Button>
    <LuMail />
    {`Button`}
  </Button>
);

export const ComponentDisabled = <Button className='disabled'>{`Button`}</Button>;

export const Wrapper = ({ children }) => children;

# Button

{data.button.description} 

## Usage

<Usage
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## With Icon

<Usage
  components={{
    usage: ComponentWithIcon,
    wrapper: Wrapper,
  }}
/>

## Disabled

<Usage
  components={{
    usage: ComponentDisabled,
    wrapper: Wrapper,
  }}
  disabled
/>

## Sizes

<Size
  config={cfg}
  components={{
    usage: ComponentWithIcon,
    wrapper: Wrapper,
  }}
/>

## Variants

<Variants
  config={cfg}
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Customizing

```sass filename="globals.css"
:root {
  --ex-button-cursor: pointer;
  --ex-button-font-family: var(--font-sans);
  --ex-button-font-weight: var(--font-weight-normal);

  --ex-button-font-size-sm: var(--text-xs);
  --ex-button-font-size-md: var(--text-sm);
  --ex-button-font-size-lg: var(--text-base);
  --ex-button-font-size-xl: var(--text-lg);

  --ex-button-height-sm: calc(var(--spacing) * 9);
  --ex-button-height-md: calc(var(--spacing) * 10);
  --ex-button-height-lg: calc(var(--spacing) * 11);
  --ex-button-height-xl: calc(var(--spacing) * 12);

  --ex-button-gap-x-sm: calc(var(--spacing) * 1.75);
  --ex-button-gap-x-md: calc(var(--spacing) * 2);
  --ex-button-gap-x-lg: calc(var(--spacing) * 2.25);
  --ex-button-gap-x-xl: calc(var(--spacing) * 2.5);

  --ex-button-px-sm: calc(var(--spacing) * 3.5);
  --ex-button-px-md: calc(var(--spacing) * 4);
  --ex-button-px-lg: calc(var(--spacing) * 4.5);
  --ex-button-px-xl: calc(var(--spacing) * 5);

  --ex-button-radius-sm: var(--radius-sm);
  --ex-button-radius-md: var(--radius-sm);
  --ex-button-radius-lg: var(--radius-sm);
  --ex-button-radius-xl: var(--radius-sm);
}
```

## API

<Api config={cfg} />
