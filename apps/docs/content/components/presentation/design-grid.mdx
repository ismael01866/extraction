import { Box, DesignGrid } from 'extraction-ui';

import data from '../data.json';
import { cfg } from './design-grid';

export const Component = <DesignGrid className='absolute grid-cols-8 grid-rows-4' />;

export const Wrapper = ({ ...rest }) => (
  <Box className='palette-neutral variant-subtle relative h-40 w-full' {...rest} />
);

# DesignGrid

{data['design-grid'].description} <br/>By default the grid has a fixed position and you must specify the `grid-cols` and/or the `grid-rows` properties.

<Usage
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## API

<Api config={cfg} />
