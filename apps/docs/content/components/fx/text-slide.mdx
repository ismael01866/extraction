import { Button, TextSlide } from 'extraction-ui';

import data from '../data.json';
import { textSlideCfg, textSlideItemCfg, textSlideTrackCfg } from './text-slide';

export const Component = <TextSlide>Hover me</TextSlide>;

export const ComponentCompound = (
  <TextSlide>
    <TextSlide.Track>
      <TextSlide.Item>Hover me 1</TextSlide.Item>
      <TextSlide.Item aria-hidden='true'>Hover me 2</TextSlide.Item>
    </TextSlide.Track>
  </TextSlide>
);

export const ComponentWithin = (
  <Button className='group/text-slide'>
    <TextSlide>
      <TextSlide.Track>
        <TextSlide.Item>Hover me</TextSlide.Item>
        <TextSlide.Item aria-hidden="true">Hover me</TextSlide.Item>
      </TextSlide.Track>
    </TextSlide>
  </Button>

);

export const Wrapper = ({ children }) => children;

# Text Slide

{data['text-slide'].description} 

<Usage
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Compound Version

If you need more control over the children, you can use the compound version of the component.

<Usage
  components={{
    usage: ComponentCompound,
    wrapper: Wrapper,
  }}
/>

## Within Components

Use the `group/text-slide` class to apply the text slide effect within other components.

<Usage
  components={{
    usage: ComponentWithin,
    wrapper: Wrapper,
  }}
/>

## Customizing

```sass filename="globals.css"
:root {
  --ex-text-slide-delay: 0;
  --ex-text-slide-ease: var(--ease-out);
  --ex-text-slide-duration: 200ms;
}
```

## API

### Text Slide

<Api config={textSlideCfg} />

### Text Slide Track

<Api config={textSlideTrackCfg} />

### Text Slide Item

<Api config={textSlideItemCfg} />
