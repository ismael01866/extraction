import { Icon } from 'extraction-ui';
import { Callout } from 'nextra/components';
import { LuMail } from 'react-icons/lu';

import data from '../data.json';
import { cfg } from './icon';

export const Component = (
  <Icon>
    <LuMail />
  </Icon>
);

export const Wrapper = ({ children }) => children;

# Icon

{data.icon.description} 

<Callout type='warning'>
  Extraction UI doesn't provide any icons out of the box. You can use any icon
  library you like, such as
  [react-icons](https://react-icons.github.io/react-icons/),
  [lucide-react](https://lucide.dev/), or even custom SVGs. The `Icon` component
  is a simple wrapper that provides consistent styling and sizing for your
  icons.
</Callout>
<br />

<Usage
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Sizes

<Size
  config={cfg}
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Palette

<Palette
  config={cfg}
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## API

<Api config={cfg} />
