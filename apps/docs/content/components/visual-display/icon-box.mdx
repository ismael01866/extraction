import { IconBox } from 'extraction-ui';
import { LuMail } from 'react-icons/lu';

import data from '../data.json';
import { cfg } from './icon-box';

export const Component = (
  <IconBox>
    <LuMail />
  </IconBox>
);

export const Wrapper = ({ children }) => children;

# Icon Box

{data['icon-box'].description} 

<Usage
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Sizes

<Size
  config={cfg}
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Variants

<Variants
  config={cfg}
  components={{
    usage: Component,
    wrapper: Wrapper,
  }}
/>

## Customizing

```sass filename="globals.css"
:root {
  --ex-icon-box-font-size-sm: var(--text-xs);
  --ex-icon-box-font-size-md: var(--text-sm);
  --ex-icon-box-font-size-lg: var(--text-base);
  --ex-icon-box-font-size-xl: var(--text-lg);

  --ex-icon-box-size-sm: calc(var(--spacing) * 9);
  --ex-icon-box-size-md: calc(var(--spacing) * 10);
  --ex-icon-box-size-lg: calc(var(--spacing) * 11);
  --ex-icon-box-size-xl: calc(var(--spacing) * 12);

  --ex-icon-box-radius-sm: var(--radius-sm);
  --ex-icon-box-radius-md: var(--radius-sm);
  --ex-icon-box-radius-lg: var(--radius-sm);
  --ex-icon-box-radius-xl: var(--radius-sm);
}
```

## API

<Api config={cfg} />
